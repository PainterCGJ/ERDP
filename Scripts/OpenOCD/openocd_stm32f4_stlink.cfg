# OpenOCD Configuration for STM32F4xx with STLink v2 (SWD)
# 烧录器: STLink v2
# 接口: SWD
# 芯片: STM32F407xx / STM32F405xx

source [find interface/stlink.cfg]
transport select hla_swd
source [find target/stm32f4x.cfg]
adapter speed 4000
